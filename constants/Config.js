import { ServerRoot, AppName, AppVersion, localVersion, AppDisplayName, AppSlogan } from "../app.json";

export default {
	ServerRoot,
	AppName,
	AppDisplayName,
	AppVersion,
	localVersion,
	AppSlogan
};
