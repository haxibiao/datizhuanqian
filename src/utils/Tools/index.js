/*
 * @flow
 * created by wyk made in 2018-12-06 10:02:54
 */
import { syncGetter } from './adapter';
import { setRootNavigation, navigate } from './navigator';
import { NumberFormat, TimeFormat, singleImageResponse, imgsLayoutSize, imageSize, regular } from './calculate';
// import { checkNetwork } from './netWork';

export default {
    syncGetter,
    setRootNavigation,
    navigate,
    NumberFormat,
    TimeFormat,
    singleImageResponse,
    imgsLayoutSize,
    imageSize,
    regular,
    // checkNetwork
};
