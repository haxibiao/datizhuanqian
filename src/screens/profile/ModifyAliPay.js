/*
 * @flow
 * created by wyk made in 2019-03-22 12:02:09
 */
import React, { Component } from 'react';
import { StyleSheet, View } from 'react-native';
import { PageContainer, TouchFeedback, Iconfont, Row, ListItem } from '../../components';
import { Theme, PxFit, Config, SCREEN_WIDTH } from '../../utils';

class ModifyAliPay extends Component {
	render() {
		return (
			<PageContainer title="账户绑定">
				<View />
			</PageContainer>
		);
	}
}

const styles = StyleSheet.create({});

export default ModifyAliPay;
